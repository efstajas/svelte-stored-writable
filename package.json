{
  "name": "svelte-stored-writable",
  "version": "0.1.0",
  "description": "An extension of Svelte's writable that saves and restores data from localstorage automatically.",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf lib/*",
    "build": "tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "svelte": "^3.57.0",
    "typescript": "^5.0.2",
    "zod": "^3.21.4"
  }
}
